mutation updateCartGoods(
  $_id: ID!,
  $number: String!,
  $spec: String!
  ) {
  updateCartGoods(_id: $_id, input: {
    number: $number,
    spec: $spec
  }) {
    _id
    goods
    number
    spec
    created_time
    updated_time
  }
}
